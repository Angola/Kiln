# Kiln — environment configuration

# Postgres connection. Kiln keeps all tables in a dedicated `kiln` schema
# (see packages/db). Point this at your instance:
DATABASE_URL=postgresql://postgres@localhost:5432/kiln

# Base URL the MCP server embeds in the dataset URLs it returns.
KILN_WEB_URL=http://localhost:3000
